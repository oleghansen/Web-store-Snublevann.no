@model PagedList.IPagedList<Nettbutikk.admin.Models.UserInfo>
@using PagedList.Mvc;
@{
    ViewBag.Title = "ListCustomers";
    Layout = "~/Views/Shared/_Layout.cshtml";
    SelectList itemsPerPageList = DefaultValues.ItemsPerPageList;
}
<link href="~/Content/Custom/MainSheet.css" rel="stylesheet" />

<div class="jumbotron" style="padding-top:2px">
    <div class="container">
        <h3>
            <span class="glyphicon glyphicon-user"></span> Kundeoversikt
        </h3>

        <div id="menu_container">
            <div class="row" style="margin-bottom: 5px;">
                <div class="col-sm-8 col-md-8" style="margin-top: 15px">
                    <div class="form-group">
                        @using (Html.BeginForm("ListCustomers", "Customer", FormMethod.Get, new { @class = "form-group", @role = "form" }))
                        {

                            <div class="input-group pull-left">
                                @Html.TextBox("searchString", ViewBag.CurrentFilter as string, new { id = "txtSearch", @class = "form-control", placeholder = "Søk etter kunde" })
                            </div>
                            <div class="input-group-btn pull-left">
                                <button class="btn btn-primary" type="submit">
                                    <span class="glyphicon glyphicon-search"></span>
                                </button>
                            </div>



                        }
                    </div>
                </div>
                <div class="col-sm-2 col-md-2" style="margin-top: 9px">

                </div>
                <div class="col-sm-2 col-md-2" style="margin-top: 15px; padding-left: 48px">

                    <a href="@Url.Action("AddCustomer")" class="btn btn-primary" id="primaryButton">
                        <span class="glyphicon glyphicon-plus"></span> Ny kunde
                    </a>


                </div>
            </div>

            <div>
                <div id="partView" class="panel panel-default partialView">
                    <table>
                        <tr>
                            <td>
                                <b><a href="@Url.Action("ListCustomers", new { sortOrder = ViewBag.IDSortParm, currentFilter = ViewBag.CurrentFilter})">@Html.DisplayNameFor(modelItem => Model[0].id)</a></b>
                            </td>
                            <td>
                                <b><a href="@Url.Action("ListCustomers", new { sortOrder = ViewBag.FirstNameSortParm, currentFilter = ViewBag.CurrentFilter})">@Html.DisplayNameFor(modelItem => Model[0].firstname)</a></b>
                            </td>
                            <td>
                                <b><a href="@Url.Action("ListCustomers", new { sortOrder = ViewBag.LastNameSortParm, currentFilter = ViewBag.CurrentFilter})">@Html.DisplayNameFor(modelItem => Model[0].lastname)</a></b>
                            </td>
                            <td>
                                <b><a href="@Url.Action("ListCustomers", new { sortOrder = ViewBag.AddressSortParm, currentFilter = ViewBag.CurrentFilter})">@Html.DisplayNameFor(modelItem => Model[0].address)</a></b>
                            </td>
                            <td>
                                <b><a href="@Url.Action("ListCustomers", new { sortOrder = ViewBag.EMailSortParm, currentFilter = ViewBag.CurrentFilter})">@Html.DisplayNameFor(modelItem => Model[0].email)</a></b>
                            </td>
                            <td>
                                <b><a href="@Url.Action("ListCustomers", new { sortOrder = ViewBag.PostAreaSortParm, currentFilter = ViewBag.CurrentFilter})">@Html.DisplayNameFor(modelItem => Model[0].postalarea)</a></b>
                            </td>
                            <td>
                                <b><a href="@Url.Action("ListCustomers", new { sortOrder = ViewBag.PostCodeSortParm, currentFilter = ViewBag.CurrentFilter})">@Html.DisplayNameFor(modelItem => Model[0].postalcode)</a></b>
                            </td>
                        </tr>
                        @foreach (var item in Model)
                        {
                            <tr class="rows">
                                <td>
                                    @Html.DisplayFor(modelItem => item.id)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.firstname)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.lastname)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.address)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.email)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.postalarea)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.postalcode)
                                </td>
                                <td class="pull-right">
                                    <a href="@Url.Action("CustomerDetails", new { id = item.id })" title="Endre" class="btn btn-xs btn-default" id="defaultButton">
                                        <span class="glyphicon glyphicon-pencil"></span>
                                    </a>
                                        <!-- <button class="btn btn-xs btn-default" style="margin-top:0px" type="submit"><i class="glyphicon glyphicon-align-justify"></i>@Html.ActionLink("Se ordre", "ListCustomerOrders", new { id = item.id }) </button>-->
                                    <a href="@Url.Action("delete",  new { id = item.id })" title=" endre" class="btn btn-xs btn-danger" id="dangerButton">
                                        <i class="glyphicon glyphicon-trash"></i></a>
                                        <!--  @if (item.admin == false)
        {
            <button class="btn btn-xs btn-default" style="margin-top:0px" type="submit"><i class="glyphicon glyphicon-user"></i>@Html.ActionLink("Sett til Admin", "makeAdmin", new { id = item.id })</button>
        }
        else
        {
            <button class="btn btn-xs btn-default" style="margin-top:0px" type="submit"><i class="glyphicon glyphicon-user"></i>@Html.ActionLink("ta vekk som Admin", "revokeAdmin", new { id = item.id })</button>
        } -->
</tr>
                        }
                    </table>
                </div>


                <div style="margin-left:10px">
                    <div class="row">
                        <div class=" col-sm-12 col-md-12">
                            @Html.PagedListPager(Model, page => Url.Action("ListCustomers", new { page, itemsPerPage = ViewBag.CurrentItemsPerPage, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }))
                        </div>
                    </div>

                    @using (Html.BeginForm("ListCustomers", "Customer", FormMethod.Post))
                    {
                        <small>Antall per side:</small> @Html.DropDownList("ItemsPerPage", itemsPerPageList, new { @id = "ItemsPerPageList", onchange = "this.form.submit();", style = "margin-bottom: 0px" })
                    }
                </div>
            </div>
        </div>
    </div>
 </div>



